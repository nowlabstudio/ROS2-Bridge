"""
RoboClaw TCP Adapter — TCP socket transport for Basicmicro motor controllers
over USR-K6 (or similar) Ethernet-to-Serial converters.

Eliminates the need for socat, virtual serial ports, or kernel device mounts.
"""

__version__ = "0.1.0"
